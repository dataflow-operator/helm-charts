{{- if .Values.gui.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataflow-operator.gui.fullname" . }}
  labels:
    {{- include "dataflow-operator.gui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.gui.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataflow-operator.gui.selectorLabels" . | nindent 4 }}
{{- end }}
